---
"@agentcash/router": patch
---

Correct the README install instructions: `next` and `zod` are now the only peer dependencies. The x402 packages, `mppx`, `viem`, and `zod-openapi` install automatically as regular dependencies.
