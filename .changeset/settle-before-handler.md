---
"@agentcash/router": minor
---

Add `mpp.settleBeforeHandler` on `.paid()` / `.mpp()` for per-route eager MPP transaction (pull) settlement before the handler runs. x402 on the same route is unaffected.
